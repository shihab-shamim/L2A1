# 🎯 Understanding `any`, `unknown`, and `never` in TypeScript

(Plus Union & Intersection Types Explained Simply)

TypeScript-এর টাইপ সিস্টেম শিখতে গেলে সবচেয়ে বেশি কনফিউশন হয় এই তিনটা টাইপ নিয়ে: **`any`**, **`unknown`**, ও **`never`**। চলুন সহজভাবে বুঝে নেই — যেন পড়তেই বুঝে ফেলবেন।

---

## 🔥 `any` – যখন TypeScript কে বলি “তুমি চুপ থাকো”

### ✔️ কী করে?

`any` ব্যবহার করলে TypeScript আপনাকে আর কোনো type-checking করবে না। ঠিক যেন JavaScript এ ফিরে গেলেন।

### ⚠️ কখন ব্যবহার করবেন?

শুধুমাত্র শেষ বিকল্প হিসেবে। কারণ type safety পুরোটা নষ্ট হয়ে যায়।

### 🧪 উদাহরণ

```ts
let data: any;
data = 10;
data = "Shihab";
data = true;
```

> ⚠️ এখানে `data` নিয়ে TypeScript আর কোনো প্রশ্ন করবে না।

---

## 🔐 `unknown` – নিরাপদ সংস্করণ `any` এর

### ✔️ কী করে?

✔️ যেকোনো ভ্যালু রাখতে পারেন
✔️ কিন্তু ব্যবহার করার আগে type-check করতে হবে
❌ সরাসরি ব্যবহার করা যাবে না

### 🧪 উদাহরণ

```ts
let data: unknown;
data = 10;
data = "Shihab";

if (typeof data === "string") {
  console.log(data.toUpperCase());
}
```

➡️ দেখলেন? `unknown` হলে আপনাকে type-check করতে হয় — এটাই এটাকে `any` থেকে নিরাপদ করে।

---

## ⛔ `never` – এমন কিছু যা কখনো ঘটবে না

### ✔️ ব্যবহার হয় যখন:

* ফাংশন কখনো return করবে না
* সবসময় error throw করবে
* infinite loop থাকবে

### 🧪 উদাহরণ

```ts
function errorFunction(msg: string): never {
  throw new Error(msg);
}

function infiniteLoop(): never {
  while (true) {}
}
```

---

## 🏁 Quick Summary

| Type      | Meaning                          | Type-Safe?  |
| --------- | -------------------------------- | ----------- |
| `any`     | Anything allowed                 | ❌ No safety |
| `unknown` | Anything allowed but must check  | ✔️ Yes      |
| `never`   | Impossible value (never returns) | ✔️ Yes      |

---

# 🎭 Union & Intersection Types

## OR = Union Type (`|`)

একটা ভ্যালু multiple টাইপের যেকোনোটা হতে পারে।

```ts
type Id = string | number;

function printId(id: Id) {
  console.log("Your ID is:", id);
}

printId(101);
printId("A-2025");
```

➡️ এখানে `id` যেকোনো একটি টাইপ হতে পারে — string বা number।

---

## AND = Intersection Type (`&`)

একটা ভ্যালু একাধিক টাইপের সব প্রপার্টি পাবে।

```ts
type Person = {
  name: string;
  age: number;
};

type Employee = {
  employeeId: number;
  department: string;
};

type Staff = Person & Employee;

const staffMember: Staff = {
  name: "Shihab",
  age: 25,
  employeeId: 1001,
  department: "Engineering"
};
```

➡️ `Staff` টাইপে Person + Employee এর সব properties থাকা লাগবে।

---

## ✍️ Author

**MD Shihab Shamim**

